docker run -d \
  --name mosquitto \
  -p 1883:1883 \
  -v /home/user/PBL-2/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf \
  eclipse-mosquitto

Em caso de erro verificar o diretório.

para parar:
docker stop mosquitto

para iniciar:
docker start mosquitto

para excluir: 
docker rm mosquitto